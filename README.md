# randomizer
electron/angular 4 app for moms work


run dev mode:  `npm run electron`

for now to package and build: 
-`build-electron`
-copy package.json to created dist directory. cd into dist
-`electron-packager .` will build the project in the native os